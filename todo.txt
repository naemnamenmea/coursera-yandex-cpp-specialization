Notes:
    *. Инфа по cmake хранится в окне Firefox

[ ] unity build
	[ ] попробовать сгенирировать unity build проект и добавить его к решению
	[ ] получить список абсолютных путей include_directories для конкретного таргета и всех его зависимостей
		[ ] на этапе генерации, сборки или уже потом (через дополнительный вызов cmake)?
	[ ] в общем  при заданном флажке во время генерации для всех таргетов, которые имеют св-во "задание" я буду генерировать 2 bat файла, 1ый который будет содержать в себе запуск quom с прописанными include путями, 2ой делть тоже самое, только делать сборку этого файла и запускать получившийся исходник и проверять чтобы код возврата был 0
		[ ] т.е. мне нужно как-то скопировать все параметры компилятора и прочее, что я указываю при компиляции какого-то таргета (думаю это можно вынести в отдельный модуль)
[ ] тесты
	[ ] можно ли собрать исполняемое приложение как dll библиотеку - т.е. без ф-ции main() по сути или нет?
		[ ] если можно, то создать отдельный проект для тестов рядом с основным проектом и подключить его как dll
	[ ] в misc подключить тесты и сделать так чтобы они запускались при после компиляции этого проекта (как раз может тут пригодится add_tests())
	[ ] поместить BasicTest в фильтр tests/
	[ ] подключить gtest к проекту
		[ ] там можно как-то настраивать тесты (посмотреть как)
			[ ] есть такое cTest - понять что это и может это чем-то помочь мне?
[ ] остальное
	[ ] сейчас у меня не правильно строятся пути к проектам, которые расположены вне корня моего решения
	[ ] использовать макросы в cmake для того чтобы можно было выбрать нужный пояс и сгенерировать файлы *.proj и прочие именно для них, а остальные пояса не трогать
